package com.marketplace.domain.userprofile.query;

import com.marketplace.BaseRepositoryTest;
import org.junit.jupiter.api.Test;

public class UserProfileQueryRepositoryTest extends BaseRepositoryTest {

    @Test
    void testSaveUserProfileRecord() {
//        var userProfileRecord = new UserProfileRecord();
//        userProfileRecord.setUpdated(Instant.now().toString());
//        userProfileRecord.setCreated(Instant.now().toString());
//        userProfileRecord.setFirstname("Hello")
//            .setLastname("world")
//            .setDisplayname("helloWorld");
//        assertThat(userProfileRecord).isNotNull();
//        userProfileRecord.setId(null);
//           Instant.now().toString(),
//           Instant.now().toString()
//       ) ;
//            .setId(null) // save with new id

//        Optional<UserProfileRecord> optional = dslContext.insertInto(Tables.USER_PROFILE)
//            .set(record)
//            .returning(Tables.USER_PROFILE.ID)
//            .fetchOptional();
//
//        assertThat(optional).isPresent();
//        UserProfileRecord userProfileRecord = optional.get();
//
//        assertThat(userProfileRecord.getId()).isNotNull().isGreaterThan(0);
    }
}
